---
'renovate-changesets': patch
---

Grouped and multiple changeset filenames now follow the same `renovate-<sha>` convention as single changesets, preventing collisions when different updates touch the same packages.
